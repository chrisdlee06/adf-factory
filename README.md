# adf-factory
End-to-end Azure data engineering project migrating an on-prem SQL Server database to the cloud via Medallion architecture (Bronze → Silver → Gold). Uses ADF, ADLS Gen2, Databricks, and Key Vault. A single dynamic pipeline ingests, transforms, and loads analytics-ready data.
